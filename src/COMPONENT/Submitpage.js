import React from "react";
const Submitpage = () => {
  return (
    <>
      <p>jniujik</p>
      {/* } 
      
      else if (!adhar.match(adharregex)) {
        console.log("email");
        setErradhar(true);
        return false;
    }
     if (!Email.match(emilregex) || Email === "") {
      console.log("hi");
      document.getElementById("warning-4").innerHTML = " **must be fulfill";
      // setErradhar(true);
      return false;
      }
    }
        } else {
      document.getElementById("fname").value = "";
      document.getElementById("lname").value = "";
      document.getElementById("mail").value = "";
      // setErremail(false);
      // setErradhar(false);
      setImage(true);
      console.log("good");
    }

 */}
    </>
  );
};
export default Submitpage;
